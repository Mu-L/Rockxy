---
title: Capture Issues
description: Fix the most common reasons Rockxy is not capturing HTTP or HTTPS traffic, including proxy startup, client routing, and runtime-specific setup.
---

# Capture Issues

If Rockxy shows no useful traffic, the problem is usually one of four things:

- Rockxy did not start cleanly,
- the client is not using Rockxy,
- HTTPS trust is incomplete,
- or the target runtime has its own setup requirements.

## Symptom: nothing appears at all

Check:

- Rockxy is started,
- the proxy port is correct,
- the target client is actually routed through Rockxy.

## Symptom: plain HTTP appears, HTTPS does not

Go straight to [Certificates and Trust](/getting-started/certificates-and-trust) or [Certificate and HTTPS Issues](/troubleshooting/certificates-and-https-issues).

## Symptom: one tool works, another does not

That is often expected. Different tools have different proxy and trust behavior.

Use the appropriate setup page:

- [Browsers and API Clients](/setup-guides/browsers-and-api-clients)
- [Terminal Runtimes and Frameworks](/setup-guides/terminal-runtimes-and-frameworks)
- [Device Setup Issues](/troubleshooting/device-setup-issues)

## Symptom: a local runtime does not emit traffic

The process may ignore the system proxy or require runtime-specific configuration.

## Symptom: requests to `localhost` never appear

macOS sends `localhost`, `127.0.0.1`, and `::1` straight to the loopback interface and skips the system proxy, so browsers and system-proxy clients never hand that traffic to Rockxy.

- Use a `.localhost` name instead of `localhost`: macOS resolves any `<name>.localhost` to loopback without touching `/etc/hosts`, so `http://api.localhost:3000` reaches the same server and goes through the proxy.
- For terminal tools, point the tool at the proxy explicitly (for example `curl -x http://127.0.0.1:9090 http://localhost:3000/`); an explicit proxy is honored even for loopback hosts.
- Developer Setup targets for Node.js, Python, Go, and the other runtimes include a localhost sample that is already routed correctly.

## Related pages

<CardGroup cols={2}>
  <Card title="First Capture Checklist" icon="list-check" href="/getting-started/first-capture">
    Run the short diagnostic checklist first.
  </Card>
  <Card title="Helper and Proxy Issues" icon="wrench" href="/troubleshooting/helper-and-proxy-issues">
    Continue here if proxy startup itself is unreliable.
  </Card>
</CardGroup>
